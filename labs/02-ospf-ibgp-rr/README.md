# Lab 02 — OSPF underlay + iBGP-EVPN with spine route-reflectors (production)

Runnable files for this lab: `topology.clab.yml`, `apply/` (per-step snippets),
`configs/` (full per-node configs).

📖 **The complete guide is in the NetForge Labs portal:**
<https://etherhtun.github.io/netforge-labs/labs/lab-02-rr/>

Run it from the repo root:
```bash
./scripts/deploy.sh 02-ospf-ibgp-rr        # boot the fabric
./scripts/apply.sh  02-ospf-ibgp-rr all    # build every step
```
