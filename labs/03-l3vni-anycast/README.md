# Lab 03 — Anycast gateway + L3VNI (inter-subnet routing)

Runnable files for this lab: `topology.clab.yml`, `apply/` (per-step snippets),
`configs/` (full per-node configs).

⚠️ **DRAFT — pending live validation** (anycast IRB / L3VNI config).

📖 **The complete guide is in the NetForge Labs portal:**
<https://etherhtun.github.io/netforge-labs/labs/lab-03-l3vni/>

Run it from the repo root:
```bash
./scripts/deploy.sh 03-l3vni-anycast
./scripts/apply.sh  03-l3vni-anycast all
```
